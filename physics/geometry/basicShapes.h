#pragma once


#include "shape.h"

// definitions in shapeClass.cpp

Shape Sphere(double radius);

Shape Cylinder(double radius, double height);

Shape Box(double width, double height, double depth);

//Shape Wedge(double width, double height, double depth);

